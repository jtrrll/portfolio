package components

import "strings"

// A eye-catching header component.
templ Header(heading string, subheading string) {
	<div class="header">
		<h1>
			{ strings.ToUpper(heading) }
		</h1>
		<p>
			{ strings.ToLower(subheading) }
		</p>
	</div>
}
